"""Minimal setup.py for editable installs (pip install -e .).

All metadata is in pyproject.toml. This file exists only for
compatibility with older pip versions that require setup.py.
"""

from setuptools import setup

setup()
